# 龟缸架参数设计器

一个可离线运行的单文件参数化设计工具，用于计算龟缸铝型材架尺寸、下料、连接件、材料清单和报价，并导出 SVG、XLSX、STEP 等文件。

在线访问：https://geemu.github.io/turtle-rack-planner/

## 更新页面

将最新版 `龟缸架参数设计器.html` 复制为本目录的 `index.html`，提交并推送到 `main` 分支后，GitHub Pages 会自动更新。

页面中的参数保存在当前浏览器本地；换设备或清除浏览器数据后会恢复默认值。